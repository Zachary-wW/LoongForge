name: Auto Label

on:
  issues:
    types: [opened, edited]
  pull_request_target:
    # `synchronize` (a new commit was pushed) is deliberately absent: a push
    # does not change the title or body, so re-labelling would recompute the
    # same set and silently restore any label a human had removed.
    types: [opened, edited, ready_for_review]

permissions:
  issues: write
  pull-requests: write

jobs:
  auto-label:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/github-script@f28e40c7f34bde8b3046d885e986cb6290c5673b # v7.1.0
        with:
          script: |
            const payload = context.payload;
            const eventName = context.eventName;

            if (payload.sender && payload.sender.type === 'Bot') {
              console.log('Triggered by bot, skipping.');
              return;
            }

            let text = "";
            let targetNumber = null;
            let existingLabels = [];

            if (eventName === 'pull_request_target') {
              text = (payload.pull_request.title + " " + (payload.pull_request.body || "")).toLowerCase();
              targetNumber = payload.pull_request.number;
              existingLabels = payload.pull_request.labels || [];
            } else {
              const issue = payload.issue;
              text = (issue.title + " " + (issue.body || "")).toLowerCase();
              targetNumber = issue.number;
              existingLabels = issue.labels || [];
            }

            // Keyword matching only ever adds labels, it never removes them, so
            // once anything is labelled a human's triage has to win: bail out
            // instead of re-adding a label somebody deliberately removed.
            if (existingLabels.length > 0) {
              console.log('Already labelled, leaving the existing triage alone.');
              return;
            }

            const rules = [
              ['bug', ['bug', 'error', 'fail', 'crash', 'broken']],
              ['enhancement', ['feature', 'request', 'enhance', 'add support']],
              ['documentation', ['doc', 'docs', 'readme', 'documentation']],
              ['question', ['question', 'how to', 'help', 'why does']],
              ['ci', ['ci', 'workflow', 'github actions', 'pre-commit']],
              ['docker', ['docker', 'dockerfile', 'container', 'image']],
              ['xpu', ['xpu', 'kunlun', 'p800']],
              ['model', ['model', 'qwen', 'llama', 'deepseek', 'internvl', 'minimax', 'glm']],
              ['training', ['train', 'pretrain', 'sft', 'fine-tun']],
              ['checkpoint', ['checkpoint', 'ckpt', 'convert']],
            ];

            const labelsToAdd = new Set();

            for (const [label, keywords] of rules) {
              const matched = keywords.some(kw => {
                const escaped = kw.replace(/[.*+?^${}()|[\]\\]/g, '\\$&');
                const regex = new RegExp(`(^|[^a-zA-Z0-9_])${escaped}(?=[^a-zA-Z0-9_]|$)`, 'i');
                return regex.test(text);
              });
              if (matched) labelsToAdd.add(label);
            }

            if (labelsToAdd.size > 0) {
              const labels = Array.from(labelsToAdd);
              console.log(`Adding labels: ${labels.join(', ')}`);
              await github.rest.issues.addLabels({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: targetNumber,
                labels: labels,
              });
            } else {
              console.log('No matching keywords found.');
            }
